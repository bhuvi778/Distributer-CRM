{
  "name": "distri-flow",
  "version": "1.0.0",
  "description": "Sales & Distribution Management System - Digitalizing SME Ecosystem",
  "scripts": {
    "dev": "concurrently \"npm run --prefix server dev\" \"npm run --prefix client dev\"",
    "server": "npm run --prefix server dev",
    "client": "npm run --prefix client dev",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run --prefix server seed",
    "demo-users": "npm run --prefix server demo-users"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["sales", "distribution", "sme", "gst", "van-sales"],
  "license": "MIT"
}
